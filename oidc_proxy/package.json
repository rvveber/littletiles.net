{
  "name": "minecraft_auth_proxy",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --bun run --watch index.ts",
    "start": "bun --bun run index.ts",
    "some": "change",
    "build": "bun build index.ts --outdir=dist --target=bun",
    
    "preview": "bun --bun run dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^4.17.21"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@xboxreplay/xboxlive-auth": "^5.0.1",
    "cookie-parser": "^1.4.6",
    "express": "^5.1.0"
  }
}
